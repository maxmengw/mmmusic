generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String         @id @default(uuid())
  clerkId       String         @unique
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  koreanMusics  KoreanMusic[]
  youtubeMusics YouTubeMusic[]
  playlistItems PlaylistItem[]
}

model KoreanMusic {
  id          String   @id @default(uuid())
  name        String
  description String
  examples    Json
  userId      String?
  user        User?    @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// ChineseMusic and FilipinoMusic models removed during refactor; only KoreanMusic kept

model YouTubeMusic {
  id            String         @id @default(uuid())
  title         String
  artist        String
  videoId       String
  userId        String?
  user          User?          @relation(fields: [userId], references: [id], onDelete: Cascade)
  playlistItems PlaylistItem[]

  @@unique([videoId, userId])
}

model PlaylistItem {
  id             String       @id @default(uuid())
  youtubeMusicId String
  youtubeMusic   YouTubeMusic @relation(fields: [youtubeMusicId], references: [id])
  position       Int?
  name           String?
  createdAt      DateTime     @default(now())
  userId         String?
  user           User?        @relation(fields: [userId], references: [id], onDelete: Cascade)
}
